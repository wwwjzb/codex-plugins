---
name: miao-song-planner
description: Use when 用户想为妙响（汽水音乐旗下 AI 音乐创作平台，Sway5.5 模型）策划一首新歌，或要求以问卷形式逐项确定歌曲主题、歌词风格、编曲等要素并生成完整创作方案。触发场景：用户给一个主题让助手策划歌曲；用户要求从主题/曲风/情绪/人声/编配/律动等选项中逐项选择；用户要求每轮至少 5 个选项、下一轮选项随上一轮选择变化、选项不合适时可自定义补充；用户需要最终输出歌名、歌词框、风格描述、参数建议和创作蓝图。
---

# 妙响歌曲创作向导

## 核心原则

- 本技能用于妙响（Sway5.5）歌曲策划。最终输出必须可直接粘贴到妙响使用，默认以华语 R&B 场景为主。
- 问卷共 12 轮，固定顺序。每轮先依据上一轮答案从参考库筛选，展示**至少 5 个选项**；末尾始终提供「以上都不是，自定义补充」。
- 每轮选项必须因上一轮答案而变化，不得复用固定菜单。
- 用户自定义答案时：记录原话，映射到最接近的既有分支继续后续轮次；无法映射时以「主题 + 情绪」为锚点继续。
- 纯音乐处理：用户在任意轮次声明「纯音乐」时，第 4 轮（歌词风格）与第 5 轮（人声）标记为不适用，第 9 轮锁定「纯音乐 / Instrumental」；歌词框只写 `纯音乐`，风格描述不得包含任何主唱、唱法或和声人声描述。
- 一次只问一轮。不要提前展示后续轮次，不要替用户做选择。
- 硬性约束：不指定真实艺人姓名；歌词必须原创；多音字按「改写整句 → 换词 → 同音字」顺序处理；风格描述禁用高频打击与宽频噪声词汇；「明亮」按音色与混音清晰度理解，不靠高频或镲片。

## 问卷流程（12 轮）

| 轮次 | 决策内容 | 依据 | 参考文件 |
| --- | --- | --- | --- |
| 1 | 主题锚定：已给主题则确认一句话概念；未给则出主题菜单（≥6 项） | 用户输入 | theme-bank.md |
| 2 | 核心曲风（≥5 项） | 主题 | style-bank.md |
| 3 | 情绪基调（≥5 项） | 主题 + 曲风 | style-bank.md |
| 4 | 歌词风格/叙事（≥5 项） | 情绪 | lyric-guide.md |
| 5 | 主唱人声（≥5 项：音色/音域/唱法/禁用） | 曲风 + 情绪 | style-bank.md |
| 6 | 核心编配/乐器（≥5 项组合） | 曲风 | style-bank.md |
| 7 | 律动与鼓组（≥5 项方案，含高频安全替代） | 曲风 | style-bank.md, sound-guide.md |
| 8 | 制作质感/混音（≥5 项方向） | 情绪 + 明亮度 | sound-guide.md |
| 9 | 歌曲结构（≥5 项） | 曲风 | style-bank.md, lyric-guide.md |
| 10 | BPM/拍号（≥5 组：速度 + 拍号 + 重拍说明） | 曲风 + 结构 | style-bank.md |
| 11 | 效果器与空间（≥5 项方案，注明 Sway5.5 安全做法） | 情绪 + 人声 | sound-guide.md |
| 12 | 段落发展（≥5 条路线，含各段鼓组/和声/人声变化） | 结构 + 情绪 | style-bank.md, lyric-guide.md |

### 每轮交互规范

1. 用编号列出选项（如 `1. 深夜都市`），并给每个选项一句话说明。
2. 选项末尾固定追加 `N+1. 以上都不是，自定义补充`。
3. 提示用户「回复编号，或输入你自己的答案」。
4. 用户选编号 → 记录选项名与完整配置（音色、禁用项、速度等）。
5. 用户自定义 → 记录原话，说明映射到的分支（如「我按 当代R&B-温柔低男声 分支继续」），然后进行下一轮。

## MiniMax 试听预览（同源翻唱链）

问卷通过「同源翻唱链」保证每一轮试听与最终成品是同一首歌：旋律与人声在锚点版确定后锁定，之后每轮只叠加风格参数，最终成品从链上最后一版翻唱而来，试听与成品的血缘关系由链路保证。

### 关键轮次定义

- 第 1 轮（主题）、第 2 轮（曲风）、第 3 轮（情绪）、第 5 轮（人声）、第 6 轮（编配/乐器）、第 7 轮（律动/鼓组）、第 10 轮（BPM/拍号），共 7 个关键轮次。
- 若用户已声明「纯音乐」，第 5 轮（人声）不适用，跳过该轮预览；第 6 轮（编配）仍生成。
- 用户自定义答案时同样生成预览（用映射后的分支配置组装 prompt）。

### 第 1 轮：哼唱方向试听 + 锚点完整版

1. **哼唱方向试听**（非链式，30 秒 wav）：prompt = 主题 + 默认 R&B 基调，`--lyrics` 传哼唱衬词（`(Ooh, ooh)` / `La la la` / `Na na na`），从 0 秒截取。用于先听旋律方向。
2. **锚点完整版**（同源链起点，完整时长）：prompt = 主题 + 基础风格描述，`--lyrics` 传**有实词的占位歌词**（与歌曲语言一致，4-8 行，可含一句主题 Hook，如「Dance with the tide」），`--seconds 0` 保留完整。
3. 播放两段给用户：哼唱版按 30 秒试听，锚点版播放完整。

### 第 2/3/5/6/7/10 轮：翻唱链

1. 用 `--cover-base` 把**上一版链文件**翻唱为新参数版本：

   ```
   <python> <skill目录>/scripts/preview.py --cover-base "D:\AI MUSIC\previews\chain_r<N-1>_*.mp3" --prompt "保留原曲旋律与歌词，加入：<本轮及之前已选参数>" --lyrics "<占位歌词（沿用）>" --seconds 0 --tag chain_r<N> --out "D:\AI MUSIC\previews"
   ```

2. 每轮 prompt 以「保留原曲旋律与歌词，加入：…」开头，把已积累参数（主题 → 曲风 → 情绪 → 人声 → 编配 → 律动/鼓组 → 制作质感/混音 → 歌曲结构 → BPM/拍号）逐轮追加，长度 ≤300 字符。
3. 播放完整翻唱版（翻唱输出时长跟随参考，不做 30 秒裁剪）。

### 最终成品

1. 用 `--cover-base "D:\AI MUSIC\previews\chain_r10_*.mp3"`，`--prompt` 传最终风格描述（≤300 字符），`--lyrics` 传完整最终歌词。
2. 翻唱歌词长度限制 10–1000 字符；完整歌词超限时使用精简结构版（保留 V1 / Pre / Ch / V2 / Ch / Bridge / Final Ch / Outro，去掉重复预副歌与哼唱衬词）。
3. 成品由链上最后一版翻唱而来，与全部试听同源。

> 时长经验：翻唱输出时长跟随参考音频（短锚点 → 短翻唱）。免费模型直接生成（非翻唱）时，输出时长由歌词完整度决定：短占位歌词约 30 秒，完整结构化歌词可到 3 分钟以上（2026-08-13 实测 3:35）。需要 3 分钟成品时，用完整结构化歌词直接生成，而不是从短锚点翻唱。

### 翻唱链注意事项

- 翻唱预处理要求参考音频能识别出人声歌词（ASR）；纯哼唱（Ooh/La la）会被判定为无人声而拒绝，因此**链上参考必须是有实词的版本**，哼唱只用于第 1 轮方向试听。
- 每轮翻唱失败时：回退用上一版链文件继续，并把该轮参数并入下一轮 prompt；1002 限流等 20 秒重试一次；1008 提示充值；不阻塞问卷。
- 翻唱输出为 MP3 完整版，保存在 `D:\AI MUSIC\previews\`，命名 `chain_r<N>_<时间戳>.mp3`。

### 时长控制（翻唱链长度规则）

- 翻唱输出时长跟随参考音频（实测 215 秒参考 → 219 秒输出），翻唱本身不能显著拉长歌曲；歌词可让输出比参考再长一点（实测 30 秒参考 + 921 字歌词 → 43 秒），但幅度有限。
- 因此整首歌的时长由**锚点**决定：锚点从零生成（不受参考限制），生成时使用结构完整的歌词（含重复副歌、预副歌、哼唱衬词，长度尽量接近 3500 字符上限），并检查脚本输出的 `DURATION`；若明显偏短（如 <60 秒），免费重新生成，最多 3 次，取最长一版作为链起点。
- 辅助杠杆：每轮翻唱都带上尽量长的歌词（≤1000 字符），可在参考时长基础上再延长一些。
- 时长预期：链上各版时长 ≈ 锚点时长 + 歌词加成；想要更长的成品，先把锚点做长（与上方「时长经验」一致：需要 3 分钟成品时，用完整结构化歌词直接生成锚点，而不是从短锚点翻唱）。

### 密钥与安全

- API Key 通过环境变量 `MINIMAX_API_KEY` 或 `~/.codex/miao-song-planner/config.json` 的 `api_key` 字段提供。
- **严禁**把 API Key 写进 SKILL.md、参考文件或任何提交到 GitHub 的文件。

### 预览输出与清理

- 哼唱方向试听：wav，裁剪 30 秒，从 0 秒截取（非链式）。
- 链式版本（锚点 + 各轮翻唱）：完整 MP3，不裁剪，直接播放。
- 完整曲目生成后，必须询问用户「是否保留本次问卷产生的试听文件」：
  - 用户选择保留 → 列出本次试听文件清单（含链式文件）与路径，继续。
  - 用户选择不保留 → 先展示本次问卷生成的试听文件清单，经用户确认后删除；**始终保留成品 `final_*` 与创作方案**。
  - 删除范围仅限本次问卷会话生成的预览文件；删除前必须展示清单并获得明确确认。

## 最终方案组装

问卷 12 轮结束后，按 `output-template.md` 一次性输出完整方案，包含 6 个部分：

1. 歌名：2-3 个候选 + 推荐 1 个。
2. 歌词框：完整歌词，使用英文段落标签与分段指令（`[Verse 1][soft Rhodes chords, ...]`），每段 4-8 行，段落间空行，即兴/旁白用圆括号，Hook 完整重复 2-3 次；交付前逐行做多音字检查。纯音乐作品写「纯音乐」。
3. 风格描述框：可直接粘贴的段落，顺序为 曲风/速度 → 调性/情绪 → 人声 → 乐器与律动 → 段落发展 → 混音空间 → Sway5.5 禁用清单。
4. 参数建议：人声性别、风格权重、参考音频权重（无参考音频时注明不适用）、创意发散程度，各给一句理由。
5. 创作蓝图：BPM/拍号/预计小节数/预计时长、调性、旋律轮廓、副歌四句模板与 Flow 蓝图、和声方向、前三秒抓耳入口、副歌三次回归矩阵。
6. 试听检查清单：按 sound-guide.md 与 output-template.md 给出生成后核对项。

## 歌曲归档（问卷全部完成并生成成品后）

完整曲目生成并处理完「试听保留/删除」询问后，执行归档：

1. 询问用户：给这首歌的归档文件夹起个名字（例如歌名《Dance With the Tide》）。
2. 在 `D:\AI MUSIC\songs\` 下创建 `<用户命名>` 文件夹；清洗非法字符（`\ / : * ? " < > |`），重名时追加 `-2`、`-3` 序号。
3. 把成品 `final_*` 移入该文件夹；若用户选择保留试听，把本次问卷生成的试听文件（`r<轮次>_*.wav`、`demo_*.wav`、`chain_r<N>_*.mp3`、哼唱版等）一并移入。
4. 写入三个文本文件（UTF-8）：
   - `<命名>_lyrics.txt`：歌词框完整内容
   - `<命名>_style.txt`：风格描述框完整内容
   - `<命名>_plan.md`：完整创作方案（六部分）
5. 向用户汇报归档文件夹路径与文件清单。

## 参考文件导航

- `references/theme-bank.md`：第 1 轮主题菜单、主题→曲风映射、自定义主题锚定规则。
- `references/style-bank.md`：第 2/3/5/6/7/9/10/12 轮。15 类 R&B 风格库与跨轮分支映射；用户选定曲风后只读取该风格的分节。
- `references/lyric-guide.md`：第 4/9/12 轮、歌词撰写与多音字检查。
- `references/sound-guide.md`：第 7/8/11 轮、风格描述撰写、高频噪声与明亮度规则。
- `references/output-template.md`：最终方案组装模板。

## 交付前检查清单

- [ ] 每轮都提供 ≥5 个选项，且选项随上一轮答案变化
- [ ] 每轮都有「自定义补充」入口，自定义答案被采纳并说明映射
- [ ] 歌词完成多音字逐行检查，优先改写而非同音字
- [ ] 风格描述不含高频噪声禁用词、无艺人姓名、无主观空话
- [ ] 关键轮次同源链已建立：r1 哼唱方向试听 + 锚点完整版 + r2/r3/r5/r6/r7/r10 翻唱 + 最终翻唱成品
- [ ] 链上参考均为有实词版本（哼唱仅用于方向试听）；翻唱歌词 ≤1000 字符、风格描述 ≤300 字符
- [ ] 锚点时长已检查：偏短时已重新生成取最长（翻唱链长度上限由锚点决定）
- [ ] 完整曲目生成后已询问试听文件保留/删除，并按用户选择处理
- [ ] 歌曲归档完成：成品/试听（如保留）/歌词/风格已存入用户命名的文件夹
- [ ] 输出包含歌名/歌词框/风格描述框/参数建议/创作蓝图/检查清单六部分
